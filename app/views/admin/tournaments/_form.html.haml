= form_for @admin_tournament do |f|
  - if @admin_tournament.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_tournament.errors.count, "error")} prohibited this tournament from being saved:"
      %ul
        - @admin_tournament.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
